#  LLMalloc
#### A quick and dirty memory allocator

I wanted to test my hand at writing a memory allocator with using only brk and sbrk syscalls and no mmaps. It was quite difficult at moments but it's always nice to see smth like this working!

build.sh to build a shared object !